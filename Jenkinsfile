pipeline {
    agent any

    stages {
        stage('Cloner le dépôt Git') {
            steps {
                git 'git@github.com:Najatag26/Automatisation_Project.git'
            }
        }
    }
}
